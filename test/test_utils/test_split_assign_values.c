# include "../h_test.h"

void    test_split_assign_values(void)
{
    // char	*str;
	// char    *tab;

	// str = strdup("OLA");
	// tab = split_assign_values(str, ' ');
	// free(str);
    // free(tab);
}