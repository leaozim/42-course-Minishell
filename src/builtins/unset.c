#include "../../include/minishell.h"

void    builtin_unset(void)
{
    // t_tokens	*next;
	// t_list		*node;
    // t_list      *env_node;

    // node = ms.tks;
    // env_node = ms.env;
    // while (node->next)
    // {
    //     next = (t_tokens *)node->next->content;
    //     if (error_invalid_identifier(&next, &node, "unset") == TRUE)
	// 		continue ;
    //     node = node->next;
    // }
    // ms.exit_status = 0;
}